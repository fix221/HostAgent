name: Build with PyInst

on:
  workflow_dispatch:
    inputs:
      version:
        description: '发布版本号 (留空则发布到 Beta)'
        required: false
        type: string
      target_platform:
        description: '目标平台'
        required: true
        type: choice
        options:
          - windows
          - linux
          - macos
          - all
        default: 'all'
      create_release:
        description: '是否创建 Release'
        required: true
        type: choice
        options:
          - yes
          - no
        default: 'yes'
      skip_signing:
        description: '跳过代码签名'
        required: false
        type: boolean
        default: false
  workflow_call:
    inputs:
      version:
        description: '发布版本号 (留空则发布到 Beta)'
        required: false
        type: string
        default: ''
      target_platform:
        description: '目标平台'
        required: true
        type: string
        default: 'all'
      create_release:
        description: '是否创建 Release'
        required: true
        type: string
        default: 'yes'
      skip_signing:
        description: '跳过代码签名'
        required: false
        type: boolean
        default: false

jobs:
  # ============================================================
  # 统一前端构建（只构建一次，供所有平台后端使用）
  # ============================================================
  build-front-all:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: FrontPages/package-lock.json

    - name: Install frontend dependencies
      run: |
        cd FrontPages
        npm install

    - name: Build frontend
      run: |
        cd FrontPages
        npx vite build --outDir ../BuildCache/frontend --emptyOutDir

    - name: Upload frontend artifact
      uses: actions/upload-artifact@v4
      with:
        name: frontend-dist
        path: BuildCache/frontend
        retention-days: 1

  # ============================================================
  # Windows x64 构建
  # ============================================================
  build-winnt-x64:
    needs: build-front-all
    runs-on: windows-latest
    if: inputs.target_platform == 'windows' || inputs.target_platform == 'all'
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Download frontend artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend-dist
        path: BuildCache/frontend

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r HostConfig/pipinstall.txt
        pip install pywin32 pythonnet
        pip install pyinstaller

    - name: Build with PyInst
      run: |
        python AllBuilder\build_pyinstaller.py
      env:
        SKIP_FRONTEND_BUILD: "1"

    - name: Build VNC Proxy (websocketproxy)
      run: |
        cd AllBuilder
        python build_vncproxy.py

    - name: Prepare release package
      run: |
        if not exist "BuildCache\pyinstaller\Websockify" mkdir "BuildCache\pyinstaller\Websockify"
        copy "BuildCache\websockify\dist\websocketproxy.exe" "BuildCache\pyinstaller\Websockify\websocketproxy.exe"
      shell: cmd

    # ── 签名：安装 SimplySign Desktop（仅 beta/release）────────────────
    - name: Setup Certum Code Signing
      if: inputs.version != 'alpha' && !inputs.skip_signing
      shell: bash
      run: |
        chmod +x ./.github/scripts/install-simplysign.sh
        ./.github/scripts/install-simplysign.sh
        powershell -ExecutionPolicy Bypass -File "./.github/scripts/configure-simplysign-registry.ps1"
      continue-on-error: true

    # ── 签名：认证 SimplySign ─────────────────────────────────────────
    - name: Authenticate Certum
      if: inputs.version != 'alpha' && !inputs.skip_signing
      shell: bash
      env:
        CERTUM_OTP_URI: ${{ secrets.CERTUM_OTP_URI }}
        CERTUM_USERNAME: ${{ secrets.CERTUM_USERNAME }}
        CERTUM_CERTIFICATE_SHA1: ${{ secrets.CERTUM_CERTIFICATE_SHA1 }}
        CERTUM_EXE_PATH: ${{ secrets.CERTUM_EXE_PATH }}
      run: |
        for attempt in $(seq 1 10); do
          echo "Authentication attempt ${attempt}/10"
          if powershell -ExecutionPolicy Bypass -File "./.github/scripts/connect-simplySign-enhanced.ps1"; then
            echo "Authentication completed"
            exit 0
          fi
          [ "$attempt" -lt 10 ] && sleep 10
        done
        echo "ERROR: Certum authentication failed after 10 attempts"
        exit 1
      continue-on-error: true

    # ── 签名：对构建产物签名 ──────────────────────────────────────────
    - name: Sign Windows binary
      if: inputs.version != 'alpha' && !inputs.skip_signing
      shell: pwsh
      env:
        CERTUM_CERTIFICATE_SHA1: ${{ secrets.CERTUM_CERTIFICATE_SHA1 }}
      run: |
        Start-Sleep -Seconds 10
        powershell -ExecutionPolicy Bypass -File "./.github/scripts/sign-windows.ps1" -TargetDirectory "BuildCache/pyinstaller" -SkipCatGeneration

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: openidcs-client-pyinst-winnt-x64
        path: |
          BuildCache/pyinstaller/openidcs-client.exe
          BuildCache/pyinstaller/Websockify/websocketproxy.exe
        retention-days: 30

  # ============================================================
  # Windows x86 构建（暂未启用）
  # ============================================================
  # build-winnt-x86:
  #   needs: build-front-all
  #   runs-on: windows-latest
  #   if: inputs.target_platform == 'windows' || inputs.target_platform == 'all'
  #   steps:
  #   - uses: actions/checkout@v4
  #
  #   - name: Set up Python (x86)
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: '3.13'
  #       architecture: 'x86'
  #
  #   - name: Download frontend artifact
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: frontend-dist
  #       path: BuildCache/frontend
  #
  #   - name: Install dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -r HostConfig/pipinstall.txt
  #       pip install pywin32 pythonnet
  #       pip install pyinstaller
  #
  #   - name: Build with PyInst
  #     run: |
  #       python AllBuilder\build_pyinstaller.py
  #     env:
  #       SKIP_FRONTEND_BUILD: "1"
  #
  #   - name: Upload artifact
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: openidcs-client-pyinst-winnt-x86
  #       path: BuildCache/pyinstaller/openidcs-client.exe
  #       retention-days: 30

  # ============================================================
  # Linux x64 构建
  # ============================================================
  build-linux-x64:
    needs: build-front-all
    runs-on: ubuntu-latest
    if: inputs.target_platform == 'linux' || inputs.target_platform == 'all'
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Download frontend artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend-dist
        path: BuildCache/frontend

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y build-essential

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r HostConfig/pipinstall.txt
        pip install pyinstaller

    - name: Build with PyInst
      run: |
        python ./AllBuilder/build_pyinstaller.py
      env:
        SKIP_FRONTEND_BUILD: "1"

    - name: Build VNC Proxy (websocketproxy)
      run: |
        cd AllBuilder
        python build_vncproxy.py

    - name: Prepare release package
      run: |
        mkdir -p BuildCache/pyinstaller/Websockify
        cp BuildCache/websockify/dist/websocketproxy BuildCache/pyinstaller/Websockify/websocketproxy
        chmod +x BuildCache/pyinstaller/Websockify/websocketproxy

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: openidcs-client-pyinst-linux-x64
        path: |
          BuildCache/pyinstaller/openidcs-client
          BuildCache/pyinstaller/Websockify/websocketproxy
        retention-days: 30

  # ============================================================
  # Linux ARM64 构建（暂未启用）
  # ============================================================
  # build-linux-a64:
  #   needs: build-front-all
  #   runs-on: ubuntu-24.04-arm
  #   if: inputs.target_platform == 'linux' || inputs.target_platform == 'all'
  #   steps:
  #   - uses: actions/checkout@v4
  #
  #   - name: Set up Python
  #     uses: actions/setup-python@v4
  #     with:
  #       python-version: '3.13'
  #
  #   - name: Download frontend artifact
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: frontend-dist
  #       path: BuildCache/frontend
  #
  #   - name: Install system dependencies
  #     run: |
  #       sudo apt-get update
  #       sudo apt-get install -y build-essential
  #
  #   - name: Install Python dependencies
  #     run: |
  #       python -m pip install --upgrade pip
  #       pip install -r HostConfig/pipinstall.txt
  #       pip install pyinstaller
  #
  #   - name: Build with PyInst
  #     run: |
  #       python ./AllBuilder/build_pyinstaller.py
  #     env:
  #       SKIP_FRONTEND_BUILD: "1"
  #
  #   - name: Upload artifact
  #     uses: actions/upload-artifact@v4
  #     with:
  #       name: openidcs-client-pyinst-linux-a64
  #       path: BuildCache/pyinstaller/openidcs-client
  #       retention-days: 30

  # ============================================================
  # macOS a64 构建
  # ============================================================
  build-macos-a64:
    needs: build-front-all
    runs-on: macos-latest
    if: inputs.target_platform == 'macos' || inputs.target_platform == 'all'
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.13'

    - name: Download frontend artifact
      uses: actions/download-artifact@v4
      with:
        name: frontend-dist
        path: BuildCache/frontend

    - name: Install Python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r HostConfig/pipinstall.txt
        pip install pyinstaller

    - name: Build with PyInst
      run: |
        python ./AllBuilder/build_pyinstaller.py
      env:
        SKIP_FRONTEND_BUILD: "1"

    - name: Build VNC Proxy (websocketproxy)
      run: |
        cd AllBuilder
        python build_vncproxy.py

    - name: Prepare release package
      run: |
        mkdir -p BuildCache/pyinstaller/Websockify
        cp BuildCache/websockify/dist/websocketproxy BuildCache/pyinstaller/Websockify/websocketproxy
        chmod +x BuildCache/pyinstaller/Websockify/websocketproxy

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: openidcs-client-pyinst-macos-a64
        path: |
          BuildCache/pyinstaller/openidcs-client
          BuildCache/pyinstaller/Websockify/websocketproxy
        retention-days: 30

  # ============================================================
  # 发布 Release
  # ============================================================
  release-all-pub:
    needs: [build-winnt-x64, build-linux-x64, build-macos-a64]
    runs-on: ubuntu-latest
    if: always() && inputs.create_release == 'yes' && !cancelled() && !contains(needs.*.result, 'failure')
    steps:
    - name: Download all artifacts
      uses: actions/download-artifact@v4

    - name: Prepare release files
      run: |
        mkdir -p release
        # Windows: 打包为 tar.gz（包含 Websockify 目录）
        if [ -d "openidcs-client-pyinst-winnt-x64" ]; then
          cd openidcs-client-pyinst-winnt-x64
          tar -czf ../release/openidcs-client-pyinst-winnt-x64.tar.gz openidcs-client.exe Websockify/
          cd ..
        fi
        # Linux: 打包为 tar.gz（包含 Websockify 目录）
        if [ -d "openidcs-client-pyinst-linux-x64" ]; then
          cd openidcs-client-pyinst-linux-x64
          chmod +x openidcs-client
          chmod +x Websockify/websocketproxy
          tar -czf ../release/openidcs-client-pyinst-linux-x64.tar.gz openidcs-client Websockify/
          cd ..
        fi
        # macOS: 打包为 tar.gz（包含 Websockify 目录）
        if [ -d "openidcs-client-pyinst-macos-a64" ]; then
          cd openidcs-client-pyinst-macos-a64
          chmod +x openidcs-client
          chmod +x Websockify/websocketproxy
          tar -czf ../release/openidcs-client-pyinst-macos-a64.tar.gz openidcs-client Websockify/
          cd ..
        fi

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ inputs.version == 'alpha' && 'alpha' || (inputs.version != '' && format('v{0}', inputs.version) || 'beta') }}
        name: ${{ inputs.version == 'alpha' && 'Alpha Release' || (inputs.version != '' && format('Release v{0}', inputs.version) || 'Beta Release') }}
        files: |
          release/openidcs-client-pyinst-winnt-x64.tar.gz
          release/openidcs-client-pyinst-linux-x64.tar.gz
          release/openidcs-client-pyinst-macos-a64.tar.gz
        fail_on_unmatched_files: false
        draft: false
        prerelease: ${{ inputs.version == '' || inputs.version == 'alpha' }}
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
